/**
 * The single display fallback name for a conversation whose title is still empty (a fresh, untitled
 * draft). Shared by every conversation surface (the list link, the batch-delete checkbox, the delete
 * confirm) so the user-facing string is defined once and cannot drift between them.
 */
export const UNTITLED_CONVERSATION_LABEL = 'Untitled conversation';
