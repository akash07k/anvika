/**
 * The default owner identity. Owner never crosses the server-client boundary;
 * choosing the default identity is a server responsibility, so this is a server-side constant
 * and is deliberately NOT part of the shared contract.
 */
export const OWNER_LOCAL = 'local';
